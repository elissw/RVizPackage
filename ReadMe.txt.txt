This is an R package customised to plot Mowiz output files.

Functions & how to use:
------------------------
You can always use ? and the function name as in general in R to get help
To see all function names do >list_package_functions()
